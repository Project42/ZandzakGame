import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class niks here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class niks extends Actor
{
    /**
     * Act - do whatever the niks wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
    }
    
    public niks(){
        getImage().scale(6, 6);
    }
}
